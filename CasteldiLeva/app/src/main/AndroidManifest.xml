<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.ilfuma.rc.casteldileva">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeAclonica_noActionBar">
        <activity android:name=".DealsActivity"></activity>
        <activity android:name=".HowItWorksActivity" />
        <activity android:name=".ShopsActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".ShopActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".WhoWeAreActivity" />
        <activity android:name=".YourAccountActivity" />
        <activity android:name=".YourCouponActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>